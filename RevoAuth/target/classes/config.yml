############
# RevoAuth #
# V: 0.1.5 #
############

##################
# PLUGIN OPTIONS #
##################

#MESSAGE PREFIX
prefix: "&6[&aRAuth&6]&7: "

#CONNECTION TIMEOUT IN SECONDS
timeout: 60

#USER JOIN ON SERVER MESSAGE (@ is user name)
join_message: "&a@ welcome on server."

#USER QUIT FROM SERVER MESSAGE (@ is user name)
quit_message: "&a@ bye."

#SESSION ACTIVE TIME IN MINUTES
session_time: 3600

####################
# DATABASE OPTIONS #
####################

#BASE TYPE (SUPPORTED: YAML, SQL)
base_type: yaml

#SQL OPTIONS (SUPPORTED: MYSQL, CONFIGURE MYSQL EXAMPLE)
sql.url: "jdbc:mysql://localhost:3306/revoauth"
sql.user: "root"
sql.password: ""

#########################
# EMAIL SERVICE OPTIONS #
#########################

#EMAIL CONFIGURAIOTN (CONFIGURE GMAIL EXAMPLE)
mail.smtp.host: "smtp.gmail.com"
mail.smtp.port: "587"
mail.smtp.starttls.enable: "true"
mail.smtp.auth: true
mail.user: "LOGIN"
mail.password: "HASLO"

#DEFAULT PASSWORD AFTER RECOVERING BY EMAIL
default_password: "1234"